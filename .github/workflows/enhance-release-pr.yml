name: Enhance Release PR

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
  workflow_dispatch: # Added to allow manual triggering

# Cancel in-progress runs when a new commit is pushed to the same PR
concurrency:
  group: enhance-release-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  enhance-release-pr:
    name: 🤖 Claude AI Enhancement
    if: startsWith(github.event.pull_request.head.ref, 'release-please--')
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout PR Branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          submodules: recursive  # Include openapi_retrofit_generator submodule
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Extract Version from PR Title
        id: version
        run: |
          # Extract version using sed -E (extended regex, more portable than grep -P)
          VERSION=$(echo "${{ github.event.pull_request.title }}" | sed -E 's/.*([0-9]+\.[0-9]+\.[0-9]+).*/\1/' || echo "")
          if [ -z "$VERSION" ]; then
            echo "⚠️ Could not extract version from PR title"
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "📦 Detected version: $VERSION"
          fi

      - name: ⬅️ Get Previous Version
        id: prev_version
        if: steps.version.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🔍 Determining previous version..."
          
          # Method 1: Try to get from base branch's manifest (most reliable)
          echo "📋 Checking base branch (${{ github.event.pull_request.base.ref }}) manifest..."
          BASE_MANIFEST=$(gh api repos/${{ github.repository }}/contents/.release-please-manifest.json?ref=${{ github.event.pull_request.base.ref }} 2>/dev/null | jq -r '.content' | base64 -d 2>/dev/null || echo "")
          if [ -n "$BASE_MANIFEST" ]; then
            PREV=$(echo "$BASE_MANIFEST" | jq -r '."."' 2>/dev/null || echo "")
            if [ -n "$PREV" ] && [ "$PREV" != "null" ] && [ "$PREV" != "0.0.0" ]; then
              echo "✅ Found version from base branch manifest: $PREV"
              echo "prev_version=$PREV" >> $GITHUB_OUTPUT
              echo "📦 Previous version: $PREV"
              exit 0
            fi
          fi
          
          # Method 2: Get latest GitHub release tag
          echo "🏷️ Checking GitHub releases..."
          LATEST_RELEASE=$(gh api repos/${{ github.repository }}/releases/latest 2>/dev/null | jq -r '.tag_name' | sed 's/^v//' || echo "")
          if [ -n "$LATEST_RELEASE" ] && [ "$LATEST_RELEASE" != "null" ]; then
            echo "✅ Found version from GitHub releases: $LATEST_RELEASE"
            echo "prev_version=$LATEST_RELEASE" >> $GITHUB_OUTPUT
            echo "📦 Previous version: $LATEST_RELEASE"
            exit 0
          fi
          
          # Method 3: Get latest tag (even if no release exists)
          echo "🏷️ Checking Git tags..."
          LATEST_TAG=$(git ls-remote --tags origin | sed -nE 's|.*refs/tags/v([0-9]+\.[0-9]+\.[0-9]+).*|\1|p' | sort -V | tail -1 || echo "")
          if [ -n "$LATEST_TAG" ]; then
            echo "✅ Found version from Git tags: $LATEST_TAG"
            echo "prev_version=$LATEST_TAG" >> $GITHUB_OUTPUT
            echo "📦 Previous version: $LATEST_TAG"
            exit 0
          fi
          
          # Method 4: Fallback to PR branch manifest
          echo "📋 Checking PR branch manifest (fallback)..."
          PREV=$(jq -r '."."' .release-please-manifest.json 2>/dev/null || echo "0.0.0")
          if [ "$PREV" = "null" ] || [ -z "$PREV" ]; then
            PREV="0.0.0"
          fi
          
          if [ "$PREV" = "${{ steps.version.outputs.version }}" ]; then
            echo "⚠️ PR branch has same version as proposed, attempting to infer previous..."
            MAJOR=$(echo "$PREV" | cut -d. -f1)
            MINOR=$(echo "$PREV" | cut -d. -f2)
            PATCH=$(echo "$PREV" | cut -d. -f3)
            if [ "$PATCH" -gt 0 ]; then
              PREV="$MAJOR.$MINOR.$((PATCH - 1))"
              echo "📉 Decremented patch version: $PREV"
            else
              PREV="0.0.0"
            fi
          fi
          
          echo "prev_version=$PREV" >> $GITHUB_OUTPUT
          echo "📦 Previous version: $PREV"

      - name: ✅ Check if Already Enhanced
        id: check
        if: steps.version.outputs.skip != 'true'
        run: |
          if grep -q "### 🚀 Release Highlights" CHANGELOG.md; then
            VERSION_LINE=$(grep -n "## \[${{ steps.version.outputs.version }}\]" CHANGELOG.md | head -1 | cut -d: -f1 || echo "0")
            HIGHLIGHTS_LINE=$(grep -n "### 🚀 Release Highlights" CHANGELOG.md | head -1 | cut -d: -f1 || echo "99999")
            
            if [ "$VERSION_LINE" != "0" ] && [ "$HIGHLIGHTS_LINE" -gt "$VERSION_LINE" ]; then
              NEXT_VERSION=$(grep -n "## \[" CHANGELOG.md | grep -v "^$VERSION_LINE:" | head -1 | cut -d: -f1 || echo "99999")
              if [ "$HIGHLIGHTS_LINE" -lt "${NEXT_VERSION:-99999}" ]; then
                echo "✅ Already enhanced"
                echo "already_enhanced=true" >> $GITHUB_OUTPUT
                exit 0
              fi
            fi
          fi
          echo "already_enhanced=false" >> $GITHUB_OUTPUT

      - name: 🤖 Full Release Enhancement with Claude
        if: steps.version.outputs.skip != 'true' && steps.check.outputs.already_enhanced != 'true'
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY_GLOBAL_CLOUD_RUNTIME }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prompt: |
            REPO: ${{ github.repository }}
            PROPOSED_VERSION: ${{ steps.version.outputs.version }}
            PREVIOUS_VERSION: ${{ steps.prev_version.outputs.prev_version }}
            PR_BRANCH: ${{ github.event.pull_request.head.ref }}
            PR_NUMBER: ${{ github.event.pull_request.number }}
            
            # 🤖 CLAUDE CODE: RELEASE MANAGER FOR open-runtime/openai_and_azure_openai_dart_client
            
            ## Project Context
            
            **OpenAI & Azure OpenAI Dart Client** is a **type-safe Dart client** for OpenAI and Azure OpenAI APIs with unified interface.
            
            Key characteristics:
            - **Dual Provider Support**: Unified interface for both OpenAI and Azure OpenAI
            - **Auto-Generated SDK**: Generated from OpenAPI specs using custom generator
            - **Smart Retry & Fallback**: Automatic retry with exponential backoff, fallback key support
            - **SSE Streaming**: Full Server-Sent Events support for streaming responses
            - **Token Counting**: Integration with tiktoken for GPT-4o, o1 models
            - **Advanced Code Generation**: Handles complex edge cases (discriminated unions, primitive unions, etc.)
            
            Key files:
            - `lib/src/unified/` - Unified client interface, configuration
            - `lib/src/shared/` - SSE, request/response mappers, helpers
            - `lib/src/generated/` - Auto-generated SDK (12K+ files from OpenAPI specs)
            - `openapi_retrofit_generator/` - Forked generator with edge case fixes
            - `OPENAPI_EDGE_CASES.md` - Comprehensive documentation of generator edge cases
            - `test/` - Integration tests (205 passing)
            
            Release Please has proposed version **${{ steps.version.outputs.version }}** (from ${{ steps.prev_version.outputs.prev_version }}).
            
            ---
            
            ## 🎯 YOUR MISSION: Complete Pre-Release Enhancement
            
            Execute these tasks IN ORDER:
            
            ---
            
            ## TASK 0: 🔢 VERSION REVIEW (CRITICAL - DO THIS FIRST!)
            
            ```bash
            git log v${{ steps.prev_version.outputs.prev_version }}..HEAD --oneline 2>/dev/null || git log --oneline -20
            ```
            
            **Version Rules:**
            | Change Type | Version | Examples |
            |-------------|---------|----------|
            | **MAJOR** | X.0.0 | Breaking API changes, removed methods, signature changes |
            | **MINOR** | x.Y.0 | New features, new API endpoints, new generator capabilities |
            | **PATCH** | x.y.Z | Bug fixes, generator fixes, edge case handling, docs, CI |
            
            **⚠️ BREAKING CHANGE Rules (IMPORTANT!):**
            
            Only use MAJOR bump for **actual API breaking changes**:
            - ✅ Changing UnifiedOpenAIClient signature
            - ✅ Removing public API methods
            - ✅ Changing request/response models in breaking ways
            
            **NEVER** MAJOR for:
            - ❌ Workflow/CI changes
            - ❌ Documentation updates
            - ❌ Generator improvements that don't break existing code
            - ❌ Regenerating SDK with cleaner names (if API-compatible)
            - ❌ Adding new features (that's MINOR)
            - ❌ Edge case fixes (usually PATCH)
            
            **If version is wrong** (e.g., 2.0.0 for just regeneration):
            1. Update `.release-please-manifest.json` to correct version
            2. Update `pubspec.yaml` version field
            3. Update CHANGELOG.md version header
            4. Commit: `fix: Adjust version to X.Y.Z (from proposed A.B.C)`
            
            ---
            
            ## TASK 1: 📝 ADD RELEASE HIGHLIGHTS
            
            Insert after version header in CHANGELOG.md:
            
            ```markdown
            ### 🚀 Release Highlights
            
            [2-4 sentences about DEVELOPER IMPACT, not implementation details]
            [Focus on: new capabilities, API improvements, generator enhancements, compatibility]
            ```
            
            **✅ GOOD Example:**
            ```markdown
            ### 🚀 Release Highlights
            
            This release improves code generation quality with cleaner enum naming (RoleRole → Role)
            and proper discriminated union support for Azure APIs. The unified client now handles
            both OpenAI and Azure with identical interfaces, including full SSE streaming support.
            All 205 integration tests pass, ensuring production-ready stability.
            ```
            
            **❌ BAD Example:**
            ```markdown
            ### 🚀 Release Highlights
            
            Various improvements and updates. Fixed some bugs and regenerated SDK.
            Please update when convenient.
            ```
            
            Commit: `docs: Add AI-enhanced release highlights for v${{ steps.version.outputs.version }}`
            
            ---
            
            ## TASK 2: 📚 DOCUMENTATION SYNC
            
            Check if these need updates:
            - `README.md` - Installation instructions, version references, usage examples
            - `CHANGELOG.md` - Ensure all changes are properly documented
            - `QUICKSTART.md` - If API changed or new features added
            - `OPENAPI_EDGE_CASES.md` - If new generator edge cases were fixed
            - `TODO.md` - If migration tasks completed or new ones identified
            
            **Only update if genuinely needed.** Don't change for the sake of it.
            
            ---
            
            ## TASK 3: 🏷️ ISSUE MANAGEMENT
            
            ```bash
            gh issue list --repo ${{ github.repository }} --state open --limit 50
            ```
            
            **When to CLOSE an issue:**
            - Commit explicitly says "Fixes #X" or "Closes #X"
            - You can verify the fix by reading code changes
            - Issue description matches what was changed
            
            **When NOT to close:**
            - Partially addressed (comment with progress instead)
            - Not 100% certain it's fixed
            - Issues needing user verification
            
            **Closing comment format:**
            ```
            🎉 This issue has been addressed in v${{ steps.version.outputs.version }}!
            
            The fix will be available once the release PR merges. You can install it via:
            
            \`\`\`yaml
            dependencies:
              runtime_openai_and_azure_openai_dart_client:
                git:
                  url: https://github.com/open-runtime/openai_and_azure_openai_dart_client
                  tag_pattern: "^v"
                version: ^${{ steps.version.outputs.version }}
            \`\`\`
            ```
            
            ---
            
            ## TASK 4: 💬 POST PR SUMMARY
            
            ```bash
            gh pr comment ${{ github.event.pull_request.number }} --body "## 🔐 Release v[VERSION] Ready!
            
            ### Version Review:
            - **Proposed:** ${{ steps.version.outputs.version }}
            - **Final:** [ACTUAL_VERSION] ✅
            - **Reason:** [Why this version is correct]
            
            ### What Claude Did:
            - [x] Reviewed version appropriateness
            - [x] Added Release Highlights to CHANGELOG.md
            - [x] Reviewed documentation (updates: yes/no)
            - [x] Processed related issues (N found)
            
            ### SDK Status:
            - **Generated Files:** 12,000+ files covering 12 OpenAPI specs
            - **Test Coverage:** 205 tests passing (11 skipped due to rate limits)
            - **Providers:** OpenAI GA/Preview, Azure GA/Preview (2024-02 through 2025-03)
            
            ### Installation (after merge):
            \\\`\\\`\\\`yaml
            dependencies:
              runtime_openai_and_azure_openai_dart_client:
                git:
                  url: https://github.com/open-runtime/openai_and_azure_openai_dart_client
                  tag_pattern: \"^v\"
                version: ^[VERSION]
            \\\`\\\`\\\`
            
            **Ready to merge!** 🎉
            
            ---
            *🤖 Generated by Claude Code Action*"
            ```
            
            ---
            
            ## EXECUTION RULES
            
            1. **Git Setup First:**
               ```bash
               git config user.name "github-actions[bot]"
               git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
               ```
            
            2. **Commit Each Change Separately** with conventional commit messages
            
            3. **Push All Changes:**
               ```bash
               git push origin ${{ github.event.pull_request.head.ref }}
               ```
            
            4. **Be Conservative** - this is a production API client used by multiple services
            
            Now execute all tasks in order!
          claude_args: |
            --allowedTools "Read,Edit,Bash(git:*),Bash(gh:*),Bash(cat:*),Bash(head:*),Bash(grep:*)"

      - name: ℹ️ Already Enhanced Notice
        if: steps.check.outputs.already_enhanced == 'true'
        run: |
          echo "ℹ️ Release PR already enhanced - skipping"


